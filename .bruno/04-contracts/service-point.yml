info:
  name: Service point by EAN
  type: http
  seq: 3
  tags:
  - data
  - live
http:
  method: GET
  url: '{{premisesBaseUrl}}/service-points/{{ENGIE_EAN}}'
  headers:
  - name: User-Agent
    value: Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/142.0.0.0 Mobile Safari/537.36
  - name: Accept
    value: application/json, application/problem+json
  auth: inherit
runtime:
  scripts:
  - type: tests
    code: |-
      // Assertions compare derived facts (types, counts, status), never response
      // values: this repo is public and Chai prints both sides of a failure.


      test("responds 200", function () {
        expect(res.getStatus()).to.equal(200);
      });

      test("path variables all resolved", function () {
        const raw = req.getUrl();
        // encodeUrl percent-encodes the braces, so an unresolved template
        // arrives as %7B%7B rather than {{. Check both forms.
        const path = raw.replace(/^https?:\/\//, '');
        let problem = 'none';
        if (path.indexOf('//') !== -1) { problem = 'empty path segment'; }
        else if (raw.indexOf('{{') !== -1 || raw.indexOf('%7B%7B') !== -1) {
          problem = 'unresolved variable';
        }
        expect(problem).to.equal('none');
      });

      test("responds with JSON", function () {
        expect(String(res.getHeader('content-type') || '')).to.contain('json');
      });

      test("returns a division", function () {
        expect(typeof res.getBody().division).to.equal('string');
      });
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 5
docs: |-
  Metadata for one metering point.

  Mirrors api.py::async_get_service_point.

  Runs after Energy contracts because that is what seeds ENGIE_EAN.
  Running it earlier drops the EAN from the path and ENGIE
  answers 404. ENGIE_EAN is a bare EAN with no _ID1 suffix.

  __init__.py calls this per EAN to tell electricity from gas: division
  is ELECTRICITY or GAS.
